---
'ultimatedarktowerboard': patch
'ultimatedarktowerdisplay': patch
'mcp-server-return-to-dark-tower': patch
---

Add a `dev:<name>` convenience script (`dev:board` / `dev:display` /
`dev:mcp-server`) as an alias for the existing demo/dev command — no
behavior change.
