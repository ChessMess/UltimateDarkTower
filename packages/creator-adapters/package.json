{
  "name": "@udtc/adapters",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "pretest": "pnpm --filter @udtc/engine build",
    "build": "echo 'adapters: no emit (type-check only for now)'",
    "typecheck": "tsc --build",
    "lint": "eslint ."
  },
  "dependencies": {
    "@udtc/engine": "workspace:*",
    "@udtc/schema": "workspace:*",
    "ultimatedarktower": "workspace:^",
    "ultimatedarktowerdata": "workspace:^",
    "ultimatedarktowerdisplay": "workspace:^",
    "ultimatedarktowerboard": "workspace:^"
  },
  "devDependencies": {
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "author": "ChessMess",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChessMess/UltimateDarkTower.git",
    "directory": "packages/creator-adapters"
  }
}
