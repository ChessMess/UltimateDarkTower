{
  "name": "ultimatedarktowerrelay-core",
  "version": "0.1.0",
  "description": "UltimateDarkTowerRelay core — headless fake BLE tower peripheral and WebSocket relay engine.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "ultimatedarktowerrelay-shared": "*",
    "@stoprocent/bleno": "^0.12.4",
    "ultimatedarktower": "^4.0.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
