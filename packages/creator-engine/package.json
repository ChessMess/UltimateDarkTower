{
  "name": "@udtc/engine",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "node test/run-all.js",
    "pretest": "tsc -b",
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0"
  }
}
