{
  "name": "@udtc/engine",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "node test/run-all.js",
    "pretest": "tsc -b",
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "ajv": "catalog:",
    "ajv-formats": "catalog:"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
