/**
 * main.ts — Vite entry point for the DarkTowerSync client web app.
 *
 * Instantiates the App controller and starts the application.
 */

import { App } from './app';

const app = new App();
app.init();
