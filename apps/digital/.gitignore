# Entries already covered by the root .gitignore (node_modules, dist, coverage,
# *.tsbuildinfo, .DS_Store, .claude/settings.local.json) are intentionally not
# repeated here — only what's genuinely specific to this app.
dist-ssr
*.local
.vite

# Editor
.vscode/*
!.vscode/extensions.json
.idea
