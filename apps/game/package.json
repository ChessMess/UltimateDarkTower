{
  "name": "ultimatedarktowergame",
  "version": "0.1.3",
  "description": "The Tower's Challenge — a solo web game driving a real Return to Dark Tower tower over Bluetooth.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run --passWithNoTests",
    "dev": "trap 'exit 0' INT TERM; vite",
    "dev:game": "pnpm run dev",
    "preview": "trap 'exit 0' INT TERM; vite preview",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "author": "ChessMess",
  "license": "MIT",
  "dependencies": {
    "@udtc/assets": "workspace:*",
    "ultimatedarktower": "workspace:^",
    "ultimatedarktowerdata": "workspace:^"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChessMess/UltimateDarkTower.git",
    "directory": "apps/game"
  }
}
