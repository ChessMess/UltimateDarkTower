# three.js and its ecosystem must be hoisted so a single copy is shared across
# display/board/creator (multiple three copies break instanceof checks).
public-hoist-pattern[]=*three*

# UDT packages declare peer ranges against each other; the workspace may carry a
# newer minor than an older peer range names. Keep peer mismatches non-fatal.
strict-peer-dependencies=false
